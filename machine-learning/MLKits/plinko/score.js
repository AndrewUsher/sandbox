const dropData = []
const
function onScoreUpdate(dropPosition, bounciness, size, bucketLabel) {
  dropData.push([dropPosition, bounciness, size, bucketLabel])
  console.log(dropData)
}

function runAnalysis() {
  // Write code here to analyze stuff
}
